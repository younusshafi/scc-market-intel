# Database — use your Render Postgres connection string in production
DATABASE_URL=postgresql://localhost:5432/scc_intel

# LLM API key for briefing generation
GROQ_API_KEY=your_groq_api_key_here

# Environment
ENVIRONMENT=development

# CORS origins (comma-separated in production)
# CORS_ORIGINS=["https://scc-intel-dashboard.onrender.com"]
